# picom-glx

Status: **abandoned**. Date: 2026-07-03 (prior session).
STUB — recorded from the project timeline only; backfill from session log.

Sequence per timeline: picom GLX error → bionic Mesa 26.1.4 rebuild
(experiments/gpu/mesa-bionic-26.1.4) → picom rebuild → finally abandoned.
Current state: picom disabled by default in the session script; xfwm4
compositing persistently off so it can be re-enabled without
double-compositing (see experiments/desktop/session-launch).
