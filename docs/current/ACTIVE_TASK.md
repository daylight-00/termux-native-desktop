# Active task: review and accept the non-executing production-capable local-supply live-evidence orchestration implementation candidate

> Task ID: `review-and-accept-non-executing-selected-provider-local-supply-live-evidence-orchestration-production-implementation-candidate-boundary`
>
> Expected state on completion: the exact six-artifact production-capable isolated-fixture orchestration candidate is accepted as bounded non-executing implementation authority without granting live-document or selected-provider-read authority.

## Objective

Review and accept only the exact candidate generated by `SELECTED-PROVIDER-LOCAL-SUPPLY-LIVE-EVIDENCE-ORCHESTRATION-PRODUCTION-IMPLEMENTATION-REVIEW-001`.

## Why now

The exact synthetic issuance, adapter and read-only evidence implementations are accepted as immutable semantic oracles. A separate production-capable implementation has now been generated and qualified against isolated temporary fixtures, so its exact bytes must be reviewed before any later live-authority design can begin.

## Required reading

- `docs/evidence/selected-provider-local-supply-live-evidence-orchestration-production-implementation-review.md`
- `docs/evidence/selected-provider-local-supply-map-evidence-transaction-implementation-boundary-acceptance.md`
- `docs/evidence/selected-provider-local-supply-evidence-live-input-adapter-execution-authorization-implementation-boundary-acceptance.md`
- `docs/evidence/selected-provider-local-supply-evidence-authorization-issuance-coordinate-production-implementation-boundary-acceptance.md`

## Pending external inputs

None. Acceptance review uses repository-owned artifacts and isolated temporary fixtures only. Live authorization and provider coordinates remain future external inputs behind separate authorization.

## Candidate boundary

- 18 explicit input bindings;
- 24 states;
- 48 ordered operations;
- 28 fail-closed contracts;
- 118 production-orchestration coverage rows;
- inherited 88/164/78 synthetic semantic authorities, 330 rows total;
- one isolated success and 28 fail-closed cases;
- 41 test-harness temporary ELF files opened and read;
- zero selected-provider opens, reads, filesystem writes, persistent replay writes or live authority.

## In scope

- reproduce all six candidate artifacts and exact SHA-256 values;
- verify accepted authority and schema source digests;
- verify independent production code does not import or invoke accepted synthetic CLIs;
- verify component `lstat`, `O_NOFOLLOW`, stable `fstat`, streaming SHA-256 and ELF64/AArch64 `DT_SONAME` surfaces using isolated temporary fixtures;
- verify all 28 failures remain fail-closed;
- verify package databases and the live glibc prefix remain invariant;
- create append-only acceptance evidence only.

## Out of scope

Live owner decisions, token issuance or activation, live coordinate receipts, execution authorization, selected-provider path opening, selected-provider byte reads, persistent replay updates, live evidence execution, local-supply-map production or acceptance, generation-root creation, population, materialization, publication, deployment or activation.

## Stop conditions

Stop if any accepted synthetic CLI is imported or invoked, any live path is rewritten into the synthetic namespace, any selected-provider path is opened, any persistent replay state is written, any authority cardinality is widened, or any protected state changes.

## Completion criteria

An exact append-only acceptance freezes the six artifacts and preserves zero live documents, zero selected-provider opens/reads, zero filesystem writes, zero persistent replay writes and zero live authority.

## Next valid action

Review and accept the exact candidate boundary only. Do not execute it against live inputs or selected-provider paths.

Do not supply, discover, open, read, localize, populate or execute against live provider inputs.

Do not acquire or populate provider bytes.
