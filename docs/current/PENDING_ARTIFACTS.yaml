schema_version: 1
state_version: 2026-07-28.15

blocking:
  - id: live-authority-transaction-input-set
    state: not-supplied-not-authorized
    note: The production implementation is accepted and an explicit owner activation statement is under separate review; the immutable live-authority transaction input set remains unsupplied and unauthorized.
  - id: owner-activation-decision
    state: qualified-explicit-candidate-acceptance-open
    note: The exact statement `바로 진행하자` is recorded for one non-executing exact input-set review transaction only; separate acceptance is required.
  - id: project-replay-registry
    state: not-supplied-not-opened-not-written
    note: The accepted implementation used only isolated fixture replay storage. No project replay-registry identity or initial integrity state is supplied.
  - id: selected-provider-coordinate-set
    state: not-supplied-not-authorized
    note: No live selected-provider coordinate set or provider byte has been supplied, opened, read or localized.
  - id: execution-authorization
    state: not-issued-not-authorized
    note: Production implementation acceptance does not issue or activate execution authorization and does not open the provider-read gate.
  - id: local-supply-map-evidence
    state: not-produced-not-authorized
    note: No live authority documents, replay tuple, selected-provider path or provider byte has been supplied, opened, read, localized or bound.

non_blocking:
  - id: downstream-application-data-service-closure
    state: intentionally-deferred-until-provider-population
    note: Fontconfig configuration/cache, locale, GSettings/accessibility services, D-Bus/TLS policy, GTK modules/loaders and the Thai dictionary remain separate capability work after the selected provider bytes are safely localized.
  - id: glibc-substrate-lifecycle
    state: intentionally-separate
    note: The glibc 2.42 hold and known 2.43 incompatibility require an independent substrate lifecycle and must not be folded into provider materialization.
