# Current project brief

> Semantic state version: `2026-07-26.07`

## Purpose and operational boundary

`termux-native-desktop` develops a native Termux/glibc workstation while separating provider authority, composition, target policy, supply evidence, generation design, population and activation. User Termux remains authoritative for execution and remote mutation.

## Current boundary

```text
claims:                              95
bounded provider roots:              31
accepted provider decision rows:     42
current-scope concrete members:      41
accepted target-policy rows:         82
existing digest-bound results:       14
legacy Drive coordinates discovered: 24
missing result coordinates:           3
legacy result-index upgrades open:   24
generation contracts defined:         7
population state:                    UNPOPULATED_SCHEMA_ONLY
intervention:                        retained
materializer design:                 blocked
activation:                          blocked
```

`RETAINED-SUPPLY-COORDINATE-GENERATION-CONTRACT-001` binds twenty-four legacy authority archives to exact Drive file IDs and outer SHA-256 values. Because those archives predate `result-index.sha256`, they are coordinate evidence rather than population-grade supply verification. Fontconfig, HarfBuzz and libxkbcommon retain missing-coordinate blockers.

The non-live generation base, same-filesystem staging/publication, resource preflight, receipt, rollback selector and failure-observability contracts are defined without creating any filesystem state.

## Current phase

The active task is `close-three-missing-result-coordinates-upgrade-legacy-indexes-and-census-member-sizes`. It is metadata-only and may not acquire, extract, copy, install, populate, deploy or activate anything.
