# 0117 — Provider-Authority Intervention Adoption and Execution Order

## Status

This record adopts `0116` as the controlling intervention for the ongoing selected-Obsidian work and converts it into the next execution order.

```text
record type:
    SESSION DIRECTION ADOPTION / EXECUTION ORDER

runtime implementation:
    NO

package operation:
    NO

generation mutation or activation:
    NO

provider-authority intervention:
    ACTIVE
```

The first selected generation remains valid immutable experiment evidence. Passive runtime validation remains accepted. Neither result is promoted into final provider-source authority.

## Current authority

Read in this order:

```text
main/docs/system-foundation/01-essence.md
main/docs/system-foundation/02-principles-and-invariants.md
main/docs/system-foundation/03-system-model-v2.md
main/docs/system-foundation/05-ideal-target-architecture.md
main/docs/system-foundation/11-architecture-reassessment-and-hard-refactor-decision.md
main/docs/system-foundation/12-document-consistency-audit-and-execution-order.md

docs/refactor/0116-end-to-end-architecture-audit-and-provider-authority-intervention.md
docs/refactor/0115-proot-oracle-supply-and-baseline-model.md
docs/refactor/0112-selected-obsidian-passive-map-selection-diagnostic-pass-and-contract-decision.md
STATUS.md
experiments/glibc/selected-obsidian-closure/README.md
```

Precedence:

```text
system-foundation
    -> constitutional intent and invariants

0116
    -> current full-project audit and intervention

0115
    -> oracle/supply/baseline vocabulary and lifecycle

0112
    -> accepted passive runtime/map facts only

STATUS + selected-closure README
    -> current work routing and stop lines
```

## Accepted evidence that remains valid

### First selected generation

```text
generation ID:
    obsidian-cpu-435ac66d15de2e9a3188

generation path:
    $HOME/gl/selected/obsidian/generations/obsidian-cpu-435ac66d15de2e9a3188

content objects:
    96

aliases:
    175

materialized bytes:
    70,897,301

structural validation:
    1851 / 1851 PASS

current:
    ABSENT
```

Interpretation:

```text
valid immutable architecture-discrimination generation:
    YES

final clean provider composition:
    NO
```

### Passive explicit-generation runtime

```text
startup/topology:
    PASS

100-second survival:
    PASS

maps capture:
    PASS

main / renderer / zygote / GPU:
    1 / 1 / 3 / 0

broad-farm mappings:
    0

rootfs-provider mappings:
    0

current mappings:
    0
```

Interpretation:

```text
selected composition can run:
    PROVEN

every selected source is final authority:
    NOT PROVEN
```

### Passive map-selection diagnosis

Accepted facts:

```text
all 96 selected content objects remain hash-correct;
Xau/Xdmcp world-selected bytes equal duplicate selected bytes;
absolute retained RPATH explains the observed source substitutions;
one Bold font is demand-unmapped rather than missing;
libX11-xcb and app-local SwiftShader require explicit CPU-map classes;
RPATH patching and existing-generation mutation remain rejected.
```

Qualification added by `0116`:

```text
observed prefix selection
    !=
final semantic provider authority
```

The operational no-patch/no-mutation decision remains valid. The final semantic owner of every selected/reference object is now subject to the complete provider-authority census.

## Direction change

Previous near-term direction:

```text
controlled pixbuf diagnostic
    -> minimum interactive capability
    -> unified successor manifest
    -> successor generation
```

Corrected direction:

```text
provider-authority census and profile/update contracts
    +
bounded non-promoting pixbuf capability discrimination
    -> authority-informed successor-manifest plan
    -> intervention-lift review
    -> only then successor materialization and activation work
```

The pixbuf diagnostic remains useful but is no longer the only blocker or a direct path to generation synthesis.

## Workstream A — provider-authority census

This is the primary workstream.

### Scope

Inventory and classify:

```text
all selected Obsidian runtime objects;
all retained/reference Obsidian runtime objects;
all relevant packages and objects under or feeding $PREFIX/glibc;
app-local providers and $ORIGIN relationships;
data capabilities used by the current application domain;
toolchain and oracle-only packages that currently coexist with runtime state.
```

### Required semantic classes

```text
WORLD_CORE_SUBSTRATE
PLATFORM_INTEGRATION_PROVIDER
GENERIC_SHARED_CAPABILITY_PROVIDER
APPLICATION_LOCAL
APPLICATION_DOMAIN_SUPPLEMENT
DATA_CAPABILITY_PROVIDER
TOOLCHAIN_ONLY
ORACLE_ONLY
MUTABLE_OR_CACHE
```

### Required row schema

Each object or capability row records:

```text
object_or_capability
current_selected_or_reference_path
current_package_or_source_provenance
semantic_class
minimum_valid_scope
application_domains
app_local_origin_relation
candidate_source_termux_glibc_package
candidate_source_exact_debian_artifact
candidate_source_upstream_or_app_local
candidate_source_project_build
candidate_source_native_termux_or_android
termux_android_adaptation_required
abi_version_coupling
profile_runtime_or_research
update_owner
revalidation_trigger
provisional_final_authority
unresolved_discriminating_evidence
```

### Decision order

For every capability:

```text
1. semantic role and minimum scope;
2. relevant reference authority;
3. candidate-source comparison;
4. preservation of valid app-local topology;
5. provider and exact supply-artifact choice;
6. update and revalidation scope;
7. successor include/reference decision.
```

Path-first conclusions are forbidden.

### Initial census groups

Start with capability-level grouping rather than 91 unrelated ELF decisions:

```text
world loader/libc/tightly coupled runtime;
Termux-aware X11/xcb and host-integration providers;
D-Bus;
GTK/GLib/GIO/Pango/ATK;
fontconfig/freetype/harfbuzz and font data;
gdk-pixbuf/image codecs/icon/MIME data;
NSS/security;
TLS and CA trust;
audio;
printing;
compression and archive libraries;
GLVND/Mesa frontend and closed graphics relations;
application-local Electron/Obsidian payload;
application-domain supplements;
toolchain/sysroot/wrappers;
oracle-only and mutable/cache state.
```

The census may retain object-level rows below each capability group.

## Workstream B — bounded interactive pixbuf diagnostic

This remains permitted as a named diagnostic/oracle capability experiment.

### Fixed boundaries

```text
same immutable unactivated generation;
no package install;
no generation mutation;
no current creation or change;
no promoted launcher change;
short receipt-owned runtime paths;
rootfs cache/modules/data as diagnostic inputs only;
closed graphics gates remain closed.
```

### Required discrimination sequence

Do not bundle all discovered inputs.

```text
D0. reproduce current vault-open failure under the named operator interaction;
D1. receipt-local relocated loader-cache effect;
D2. exact loader-module effect, if distinct from D1;
D3. icon-theme effect;
D4. MIME-database effect;
D5. combinations only when preceding single-variable results require them.
```

### Diagnostic interpretation

A PASS proves only:

```text
that named diagnostic inputs satisfy the interaction under the tested composition.
```

A PASS does not prove:

```text
rootfs provider authority;
all twelve loader modules are required;
all icon or MIME data is required;
Debian is the final source;
successor-generation membership;
clean runtime package membership.
```

Results feed the provider-authority census as discriminating evidence.

## Workstream C — runtime and research profiles

Two explicit profiles are required.

### Minimum workstation runtime profile

Target content:

```text
native Android/Termux host and session;
minimal coherent glibc world core;
promoted platform/generic capability providers;
application-local payloads and accepted domain supplements;
selected data providers;
runtime launch/status/rollback surface;
mutable user state.
```

Not included by availability alone:

```text
PRoot;
Debian oracle rootfs;
GCC/compiler packages;
binutils/sysroots/headers;
glibc-runner;
build dependencies;
oracle-only packages;
current accumulated package state.
```

### Research/build/maintenance profile

May include:

```text
PRoot tooling;
pinned oracle seed and named scenarios;
APT/dpkg acquisition and metadata tools;
gcc-glibc and explicit-loader/binutils wrappers;
headers/sysroots;
Python/Meson/Ninja and build tools;
artifact inspection;
validators and historical receipts.
```

The two profiles must have separate installation, update, and cleanup claims.

## Workstream D — update matrix

Document five domains independently.

### World substrate update

```text
candidate identity
    -> substrate self-integrity and ABI gates
    -> platform-provider compatibility
    -> generic-provider compatibility
    -> representative application gates
    -> explicit promotion and rollback boundary
```

### Provider update

```text
provider candidate and supply identity
    -> provider ABI/behavior gates
    -> dependent application domains only
    -> promotion and retained previous provider
```

### Application update

```text
new application payload
    -> app-local closure and $ORIGIN locality
    -> capability binding delta
    -> launcher/family policy
    -> passive and interactive gates
```

### Toolchain update

```text
compiler/linker/sysroot candidate
    -> toolchain smoke
    -> reproducible output artifact
    -> produced-provider validation
```

No automatic runtime mutation follows.

### Oracle-scenario update

```text
seed/scenario/package identity
    -> control behavior and artifact receipts
```

No automatic promoted-runtime mutation follows.

## Source-specific pressure

### glibc world

Only these begin as `WORLD_CORE_SUBSTRATE` candidates:

```text
dynamic loader;
libc core;
tightly coupled runtime;
world loader/configuration and ABI identity.
```

A file under `$PREFIX/glibc` does not join the class automatically.

Termux-aware X11/xcb or path-sensitive providers begin as `PLATFORM_INTEGRATION_PROVIDER` candidates.

### Generic libraries

Capabilities such as GTK, D-Bus, fontconfig, pixbuf, NSS, audio, printing, compression, and TLS require source comparison.

When both Termux-glibc and Debian candidates exist, compare:

```text
Termux/Android patches;
ABI and version coupling;
dependency breadth;
actual consumer evidence;
update lifecycle;
supply reproducibility;
behavior under the target application domains.
```

### Fonts

The four Debian-derived selected fonts remain first-generation transition evidence only.

The final font authority is a separate data-capability decision against:

```text
Latin coverage;
monospace and Bold behavior;
Korean/CJK coverage;
math coverage;
fallback behavior;
fontconfig configuration/cache ownership;
native Termux/Android/project-owned candidates.
```

### Toolchain

`gcc-glibc` and explicit-loader/binutils wrappers remain the validated `toolchain.glibc-target` implementation.

They do not enter the minimum runtime profile.

Debian GCC remains an ordinary Debian build oracle unless GCC-specific behavior is required by a named project.

`glibc-runner` remains excluded from the clean baseline until a unique runtime responsibility is proven.

## Supply authority

The successor may not depend permanently on mutable installed source paths.

Target:

```text
oracle seed/scenario or supply transaction
    -> exact package/upstream/project artifact
    -> hash/signature/version receipt
    -> selected extracted paths or reproducible generated output
    -> provider candidate
```

Not:

```text
current mutable rootfs absolute path
    -> final materialization authority
```

The first generation may continue to reference its historical source receipts as evidence. The successor plan must introduce locked supply identities.

## Work explicitly blocked

Until intervention pass conditions are met:

```text
final successor manifest;
unified successor generation materialization;
current activation;
prefix/rootfs cleanup from physical location alone;
clean runtime package-list finalization;
native font provider promotion;
pixbuf/icon/MIME provider promotion;
compiler/toolchain membership in runtime profile;
glibc-runner baseline inclusion.
```

## Intervention lift conditions

The successor block can be reviewed only after repository documentation contains:

```text
1. current glibc-prefix package/object semantic partition;
2. selected/reference Obsidian provider-authority matrix;
3. generic capability source-choice rationale;
4. minimum runtime and research/build profiles;
5. five-domain update matrix;
6. separate native font and pixbuf/icon/MIME status;
7. authority-driven successor-manifest plan;
8. unresolved evidence list;
9. locked supply-artifact strategy independent of mutable oracle trees.
```

Lifting the block requires an explicit audit decision. Completion of one diagnostic is insufficient.

## Immediate next execution order

```text
N0. onboard from 0116, 0115, 0112, STATUS, selected-closure README, and the session handoff;
N1. define census tables and capability grouping;
N2. capture/read current selected/reference and glibc-prefix package/object evidence without mutation;
N3. populate provisional semantic classes and unresolved evidence;
N4. continue the controlled pixbuf discriminator only within the bounded permission;
N5. write runtime/research profiles and update matrix;
N6. write authority-driven successor-plan inputs without finalizing a manifest;
N7. perform intervention-lift audit;
N8. only after PASS, plan successor generation materialization.
```

## Stop line

Do not:

```text
reinterpret the first generation as failed or disposable;
reinterpret passive runtime PASS as final provider authority;
continue directly from pixbuf PASS to successor generation;
make `$PREFIX/glibc`, rootfs, glibc-repo, or Debian defaults sufficient source authority;
install packages for the bounded diagnostic;
mutate the existing generation;
create or change current;
change the promoted launcher;
reopen closed graphics work;
merge compiler/oracle state into the minimum runtime profile;
delete historical oracle or generation evidence before replacement supply/scenario identities exist.
```
