# 0120 — Selected Obsidian Provider-Authority N2 Device Receipt Review

## Status

```text
N0 repository onboarding:
    PASS

N1 census schema and evidence plan:
    PASS

N2 read-only provider evidence:
    PASS / CLOSED

N3 provisional authority classification:
    READY, NORMALIZATION FIRST

provider-authority intervention:
    ACTIVE

successor manifest/materialization/current activation:
    BLOCKED
```

This record accepts the N2 evidence transaction. It does not accept a final provider authority.

## Reviewed receipt

```text
archive:
    selected-obsidian-provider-authority-n2-read-only-evidence-results-20260712-155013.tgz

independent SHA-256:
    e1eec5b68286cd6f888241afb50d9eabe00a8765269ecf20eb57bc0d7fe270d0

members:
    45

archive safety:
    PASS

captured branch:
    docs/post-graphics-architecture-audit

captured HEAD:
    b8a7ba253053c9355ce53fb3db92ae905f4855b9
```

Archive safety reconstruction found zero absolute paths, parent traversals, symlinks, or special members.

## Independent reconstruction

```text
required embedded inputs:
    28

input status failures:
    0

embedded SHA-256 mismatches:
    0

generation identity checks:
    96

generation identity failures:
    0

current before/after:
    ABSENT / ABSENT

dpkg status changed:
    NO

dpkg info metadata changed:
    NO
```

The N2 transaction is accepted as read-only for the observed device state.

## Selected/reference and diagnostic evidence

```text
selected/reference seed rows:
    161

unique row IDs and paths:
    161 / 161

duplicate row IDs or paths:
    0 / 0

pixbuf/icon/MIME supplemental rows:
    20
```

Historical action accounting remains complete:

```text
selected static ELF:
    87

required dynamic ELF:
    4

excluded graphics ELF:
    11

app-local references:
    11

world-substrate references:
    6

world-locale references:
    12

selected fonts:
    4

GSettings aggregate:
    1

mutable state:
    19

regenerated caches:
    5

optional GPU device:
    1
```

The two exact-identity Xau/Xdmcp source substitutions remain evidence about the passive map contract. They do not promote all prefix objects into world core.

## Current glibc-prefix surface

```text
observed/package-listed paths:
    27,279

contributing package identities:
    86

regular files:
    18,104

symlinks:
    8,168

directories:
    1,007

ELF files:
    958

package-listed missing paths:
    0

unowned paths:
    2

current glibc package version:
    2.42
```

The current version is a new device observation. Older version records remain valid for their historical transaction.

Only 26 of the 86 packages have selected/reference paths or retained direct-consumer edges in this graph. The other 60 are not thereby removable; the result only proves package presence and tested demand are separate claims.

## Package-control evidence

```text
.list files:
    86

.md5sums files:
    86

.conffiles files:
    8

preinst/postinst/prerm/postrm/triggers:
    0
```

Raw extraction still would not reproduce dpkg ownership, status, dependency, and conffile semantics.

## Unowned loader state

```text
$PREFIX/glibc/etc/ld.so.cache
    size: 44,684
    SHA-256: 91b45aec95233536c439ed87e11ee3e8fe2a3afc7a747fe998c6df434329f2aa

$PREFIX/glibc/etc/ld.so.conf
    size: 82
    SHA-256: 01f5b20f777259a788b98cb522a34b7e0edd62f4babdce43a518be1672365512
```

These require an explicit lifecycle owner, generation rule, update condition, and rollback boundary. Missing dpkg ownership is not itself a semantic classification.

## Census result and normalization requirement

```text
capability rows:
    25

census rows:
    26,419

unassigned.prefix-surface member rows:
    26,213

duplicate census row IDs:
    0

accepted authority decisions:
    0

all semantic classes:
    UNRESOLVED

all authority decisions:
    OPEN
```

The exhaustive census is valid raw evidence, but 26,213 independent path judgments are not an acceptable N3 decision surface.

N3 must begin in this order:

```text
N3A
    retain glibc-prefix-package-surface.tsv unchanged as raw evidence

N3B
    form object-level rows for:
        selected/reference 161
        pixbuf/icon/MIME 20
        prefix ELF 958
        unowned loader state 2
        package aggregates 86

N3C
    aggregate remaining non-runtime files by package and semantic path class:
        headers
        static archives
        pkg-config/CMake metadata
        executable tools
        configuration
        shared data/locale
        documentation
        other

N3D
    assign provisional semantic class, profile, update owner,
    candidate-source state, and unresolved discriminator
```

The raw 27,279-path table remains available for drill-down.

## Strong provisional pressure allowed in N3

```text
valid AppDir/$ORIGIN topology:
    APPLICATION_LOCAL

application state and caches:
    MUTABLE_OR_CACHE

compiler/binutils/headers/sysroot/build metadata:
    TOOLCHAIN_ONLY

pinned Debian scenario state:
    ORACLE_ONLY

font/locale/schema/icon/MIME/trust data:
    DATA_CAPABILITY_PROVIDER
```

World core, Termux/Android integration, generic shared providers, compiler runtime support, pixbuf minimum capability, fonts, and loader state still require comparison evidence.

## Archive output policy

Future wrappers must keep:

```text
unpacked evidence roots:
    stage-specific paths under $PREFIX/tmp

TGZ archives:
    $HOME/Downloads
```

## Direction decision

```text
N2:
    CLOSED / PASS

N3 entry:
    NORMALIZATION FIRST

bounded pixbuf discriminator:
    PERMITTED / NON-PROMOTING

provider-authority intervention:
    ACTIVE

next action:
    BUILD NORMALIZED N3 DECISION SURFACE
```

## Stop line

Do not:

```text
classify 26,213 unassigned paths independently;
treat all 86 packages as runtime;
treat the 26 selected-related packages as final providers;
treat every glibc-package-owned path as world core;
mutate ld.so.conf or ld.so.cache;
install or remove packages;
mutate the existing generation;
create current;
finalize or materialize a successor;
reopen closed graphics work.
```
