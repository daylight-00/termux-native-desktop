# Session-operations changelog

## 2026-07-14 — structured recurring handoff

- Separated durable collaboration rules from project-state handoffs.
- Established `session-operations/README.md` as the stable new-session initialization entry point.
- Defined execution, mandatory handoff and miscellaneous backup package classes.
- Defined the two-package close for unexecuted work or intentionally deferred result review.
- Recorded Drive local-path rewriting, first-turn restrictions, exact-folder lookup and remote byte-verification rules.
- Recorded wrapper lessons for `set -u`, bounded tests, rollback, guarded push and selective receipt inspection.
- Required operational lessons to be updated during the session instead of being copied into every dated handoff.

## 2026-07-14 — onboarding full-cycle review

- Added a canonical project-principles entry so successors understand the evidence and authority philosophy before acting.
- Added a closed receive-to-next-handoff session lifecycle.
- Required raw pending-result bytes to be included in the mandatory handoff when available, while keeping semantic review explicitly deferred.
- Made immediate Drive access a fallback rather than a prerequisite for the successor's first project action.

## 2026-07-14 — successor verification findings

- Recorded that exact branch search may miss a known slash-containing ref; branch identity is confirmed by a known-ref file read plus commit comparison.
- Recorded the fallback when the authoring container has no outbound Git transport: connector-backed remote verification plus explicitly bounded bundle recovery, without treating stale recovery material as the current baseline.
- Resolved the deferred 0164 boundary and made the reviewed HEAD, tree, result digest and next state the active handoff coordinates.
